<?xml version="1.0" encoding="UTF-8"?>
<Context>

    <!-- Default set of monitored resources -->
    <WatchedResource>WEB-INF/web.xml</WatchedResource>

    <!-- disable session persistence across Tomcat restarts -->
    <Manager pathname="" />

    <Environment name="nwc.endpoint.geoserver" value="http[s]://supply.your.own.com/geoserver/" type="java.lang.String" override="true"/>
    <Environment name="nwc.endpoint.thredds" value="http[s]://supply.your.own.com/thredds/" type="java.lang.String" override="true"/>
</Context>